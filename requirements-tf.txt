# ============================================================
# Additional dependencies for Notebook 2 (Cough Detection)
# Requires TensorFlow — install separately if needed:
#   pip install -r requirements-tf.txt
# ============================================================

tensorflow>=2.15
tensorflow-hub>=0.16
librosa>=0.10
soundfile>=0.12
joblib>=1.3
scikit-learn>=1.3
